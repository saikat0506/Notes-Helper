// 1. Create a Boolean value for an ID variable
let hasValidID = true; // You can change this to false to test the other condition

// 2. Using a ternary operator, create a message variable that will check whether their ID is valid
let message = hasValidID ? "Access granted: You may enter the venue." : "Access denied: Valid ID required.";

// 3. Output the response to the console
console.log(message);
